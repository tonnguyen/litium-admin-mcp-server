---
title: manage_product
description: 'Manage Litium products (base products)'
---

## Overview

The `manage_product` tool provides comprehensive management of base products in your Litium catalog. Use this tool to search, retrieve, create, update, and delete products, as well as get product variants.

<Note>
  **Enhanced Search:** Product search now searches across multiple fields (ID, Name, ArticleNumber) to find products more effectively.
</Note>

## Operations

<ParamField path="operation" type="string" required>
  The operation to perform. Must be one of:
  - `search` - Search for products
  - `get` - Get a specific product by ID
  - `create` - Create a new product
  - `update` - Update an existing product
  - `delete` - Delete a product
  - `get_variants` - Get variants for a product
</ParamField>

## Parameters

### For `search` operation

<ParamField path="params" type="object">
  Search parameters:
  
  <Expandable title="properties">
    <ParamField path="search" type="string">
      Search term for filtering products
    </ParamField>
    <ParamField path="skip" type="number" default="0">
      Number of items to skip (pagination offset)
    </ParamField>
    <ParamField path="take" type="number" default="20">
      Number of items to return per page
    </ParamField>
    <ParamField path="sort" type="string">
      Sort field (optional)
    </ParamField>
  </Expandable>
</ParamField>

### For `get`, `update`, `delete`, `get_variants` operations

<ParamField path="systemId" type="string" required>
  The unique system ID of the product
</ParamField>

### For `create` and `update` operations

<ParamField path="data" type="object" required>
  Product data object with product fields
</ParamField>

## Examples

### Search Products

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "search",
      "params": {
        "search": "dress",
        "take": 10
      }
    }
  },
  "id": 1
}
```

```text Natural Language
"Search for dress products in the catalog"
```
</CodeGroup>

**Tested Examples**:
- ✅ "dress" - Returns products with "dress" in ID (e.g., "courtdress", "w-woven-dress")
- ✅ "Court Dress" - Returns "Court Dress" product

### Get Product Details

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "get",
      "systemId": "abc-123-def-456"
    }
  },
  "id": 2
}
```

```text Natural Language
"Show me the product with ID abc-123-def-456"
```
</CodeGroup>

### Create Product

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "create",
      "data": {
        "name": "New Laptop",
        "sku": "LAP-001",
        "price": 999.99
      }
    }
  },
  "id": 3
}
```

```text Natural Language
"Create a new product called 'New Laptop' with SKU LAP-001 and price 999.99"
```
</CodeGroup>

### Update Product

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "update",
      "systemId": "abc-123-def-456",
      "data": {
        "price": 899.99
      }
    }
  },
  "id": 4
}
```

```text Natural Language
"Update the price of product abc-123-def-456 to 899.99"
```
</CodeGroup>

### Delete Product

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "delete",
      "systemId": "abc-123-def-456"
    }
  },
  "id": 5
}
```

```text Natural Language
"Delete the product with ID abc-123-def-456"
```
</CodeGroup>

### Get Product Variants

<CodeGroup>
```json JSON-RPC
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "manage_product",
    "arguments": {
      "operation": "get_variants",
      "systemId": "14ad979b-95dc-41ee-b653-0823361fac20"
    }
  },
  "id": 6
}
```

```text Natural Language
"Show me all variants of the M Windtherm Hood product"
```
</CodeGroup>

**Result**: Returns array of variant system IDs:
```json
["09c36fcf-55d0-49cd-8c4b-aba1f8cac6bd"]
```
**Tested Example**:
- ✅ Product `14ad979b-95dc-41ee-b653-0823361fac20` (M Windtherm Hood) has 1 variant (Size S, Black)

## Response Format

```json
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "{\"products\":[...]}"
      }
    ]
  }
}
```

## Common Use Cases

<AccordionGroup>
  <Accordion title="Product Catalog Management">
    Use `search` to find products, `get` to view details, and `update` to modify pricing or descriptions.
  </Accordion>
  
  <Accordion title="Inventory Updates">
    Search for products by SKU, then update stock levels or pricing information.
  </Accordion>
  
  <Accordion title="Product Variants">
    Get a product and use `get_variants` to see all available sizes, colors, or other variations.
  </Accordion>
</AccordionGroup>

## Related Tools

- [manage_product_variant](/tools/products/manage-product-variant) - Manage individual product variants
- [manage_product_category](/tools/products/manage-product-category) - Organize products into categories
- [manage_price_list](/tools/products/manage-price-list) - Manage pricing

<Warning>
  Use `delete` operation with caution in production environments. Always verify the systemId before deleting.
</Warning>

